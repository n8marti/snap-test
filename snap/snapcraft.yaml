
  name: test-1984
  version: git
  summary: Troubleshoot a Telecel modem connection.
  description: |
    Perform various tests to identify why your Telecel modem connection
    is not working properly.

  grade: devel # must be 'stable' to release into candidate/stable channels
  confinement: devmode # use 'strict' once you have the right plugs and slots

  parts:
    modem-check:
      # See 'snapcraft plugins'
      plugin: python3
      source: modem-check/.
  
  apps:
    ModemCheck:
      command: bin/ModemCheck
